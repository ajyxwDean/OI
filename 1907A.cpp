#include <iostream>
#include <algorithm>
#include <cstdio>
#include <cmath>
#include <vector>

using namespace std;

const int N  = 26;
const int INF = 0x7fffffff;
using ll = long long;
using LL = ll;

int T;

void solve();

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    cin >> T;
    while(T --) solve();
    return 0;
}

void solve() {
    // int line, row;
    char line;
    int row;
    line = getchar();
    row = getchar() - 'a';
    for(int i = 1; i <= row; i ++) {
        
    }

}