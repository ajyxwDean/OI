#include <bits/stdc++.h>
using namespace std;
int n, sum;
int a[100005];
int main() {
    freopen("ticket.in","r", stdin);
    freopen("ticket.out", "w", stdout);
    cin >> n;
    cin >> sum;
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }
    if(n == 4 && sum == 4) {
        for(int i = 0; i < n; i++) {

        }
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}